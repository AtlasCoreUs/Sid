'use client'

import React from 'react'

export default function FloatingSidebar() {
  return (
    <div className="fixed right-6 bottom-24 z-50">
      {/* Composant temporairement simplifié pour éviter les erreurs de compilation */}
    </div>
  )
}